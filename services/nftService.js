const anchor = require("@project-serum/anchor");

// Define the Nft type as JSON (JavaScript doesn't have native type declarations)
const Nft = {};

// Mint a new random NFT from our collection.
async function mintNFt() {
    let x = [];
    return x;
}

// Look up any NFTs that have been minted by our collection.
async function getNftsMinted() {
    let x = [];
    return x;
}

// Place a bid on an NFT.
async function bidOnNft(mintAddress, lamports) {
    let x = [];
    return x;
}

// Get all NFTs owned by your wallet.
async function getNftsOwned(publicKey) {
    let x = [];
    return x;
}

// Sell an NFT to the highest bidder.
async function sellNft(mintAddress, ownerPublicKey) {
    let x = [];
    return x;
}

module.exports = {
    mintNFt,
    getNftsMinted,
    bidOnNft,
    getNftsOwned,
    sellNft
};
